// Server removed — this project is now a static React-only application.
// The original server.ts file contained an Express server and AI agent endpoints
// which are intentionally removed to keep the repo client-side only.

// If you need a local static server for development, run:
//   npx http-server dist
// or keep esbuild's watch and open dist/index.html in a browser.

export {};

